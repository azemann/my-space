# Archives ImageGen

Ce dossier conserve les anciennes planches qui ne doivent plus être chargées
par Godot ou Tiled. Les assets actifs sont exclusivement dans
`assets/tilesets/`.

Le sous-dossier `tilesets/` contient les versions `v001` remplacées par les
planches de production raccordables `v002`.
