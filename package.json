{
  "name": "assetforge",
  "version": "0.1.0",
  "description": "Pipeline générique de production d’assets visuels, avec une mémoire artistique locale à chaque projet.",
  "type": "module",
  "bin": {
    "assetforge": "./bin/assetforge.js"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check bin/assetforge.js && node --check src/cli.js && node --test --test-isolation=none"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "sharp": "^0.35.3"
  }
}
